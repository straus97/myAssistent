# Environment Configuration Template
# Скопируйте этот файл в .env и заполните своими значениями

# Environment
ENV=dev

# API Settings
API_BASE_URL=http://127.0.0.1:8000
API_KEY=your_api_key_here_generate_with_openssl_rand_hex_32

# Database Configuration
# SQLite (по умолчанию для разработки):
DATABASE_URL=sqlite:///./assistant.db

# PostgreSQL (для продакшна, запустите docker-compose up -d postgres):
# DATABASE_URL=postgresql://myassistent:change_me_in_production@localhost:5432/myassistent

# PostgreSQL через pgbouncer (рекомендуется для high-load):
# DATABASE_URL=postgresql://myassistent:change_me_in_production@localhost:6432/myassistent
# USE_PGBOUNCER=true

# PostgreSQL connection pooling (игнорируется для SQLite)
DB_POOL_SIZE=20
DB_MAX_OVERFLOW=10
DB_POOL_RECYCLE=3600

# PostgreSQL credentials (для docker-compose)
POSTGRES_PASSWORD=change_me_in_production
PGADMIN_PASSWORD=admin

# Logging
LOG_DIR=./logs
LOG_LEVEL=INFO

# Artifacts & Models
ARTIFACTS_DIR=./artifacts

# Telegram Notifications
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_from_botfather
TELEGRAM_CHAT_ID=your_chat_id_number

# News API (optional)
NEWS_API_KEY=

# Bybit Exchange API (для реальной торговли - пока не используется)
BYBIT_API_KEY=
BYBIT_API_SECRET=
BYBIT_TESTNET=true

# Trade Settings
TRADE_MODE=live
# Options: live, close_only, locked

# UI Settings
OFFLINE_DOCS=1
ENABLE_DOCS=1

# Automation Settings
APP_AUTOMATION_CONCURRENCY=1
PRICES_EVERY_MIN=3
MODELS_DAILY_UTC=03:20
REPORT_DAILY_UTC=03:50
HORIZON_STEPS=12

# Discovery Settings
DISCOVER_MIN_VOL_USD=2000000
DISCOVER_TOP_N=25
DISCOVER_QUOTES=USDT
DISCOVER_TIMEFRAMES=15m
DISCOVER_LIMIT=1000
DISCOVER_EXCHANGES=binance,bybit

# Timezone
TZ=UTC

