# myAssistent Project Rules

## 📋 Документация и контекст
- Перед любым действием открой /docs/, в первую очередь /docs/NEXT_STEPS.md
- Синхронизируй понимание текущих целей, ограничений, определений терминов и статуса задач
- Если в /docs/NEXT_STEPS.md есть расхождения с кодом — зафиксируй это в плане и предложи коррекцию файла

## 🎯 Планирование
- Перед правкой — план действий
- Всегда сначала напиши краткий, нумерованный план: какие файлы менять, какие интерфейсы или эндпоинты затрагиваются, риски, обратимые шаги, критерии готовности и как валидировать в Swagger
- После согласования плана выполняй изменения строго по шагам плана

## 🗂️ Организация работы
- Одна большая задача = один чат
- На один чат берётся одна крупная цель, issue или эпик
- Включай контекст только релевантных файлов через Add context, исключай лишнее
- Если задача расходится по веткам — выделяй подзадачи либо заводи новый чат

## 📝 Формат ответов
- Только прикладные патчи
- Каждый ответ должен содержать блоки в строгом порядке: Файл → Якорь → Сделать → Патч → Пояснение → Шаги проверки в Swagger
- **Файл** — абсолютный путь и существование файла
- **Якорь** — точная привязка (строки, функция, селектор, заголовок), чтобы найти место правки без поиска
- **Сделать** — что именно изменить (кратко)
- **Патч** — полноценный diff, вставка или замена без плейсхолдеров, готовый к копипасте
- **Пояснение** — почему так, влияние на контракт, производительность, безопасность
- **Шаги проверки в Swagger** — пошагово: какие эндпоинты открыть, какие поля ввести, ожидаемые коды и ответы, негативные кейсы

## 🚀 Деплой
- После изменений, добавления или редактирования файлов выполняй git push в целевую ветку
- Затем запусти обновление на сервере командой update-myassistent
- Приложи лог или ссылку на пайплайн и результат деплоя

## 📚 Документация
- После каждого шага обновляй память инструмента и разделы в /docs/
- Фиксируй что сделано, что осталось, решённые и открытые риски, команды запуска и проверки
- Синхронизируй /docs/NEXT_STEPS.md с актуальным состоянием

## 🛠️ Технологии проекта
### Backend
- FastAPI + Python 3.11
- SQLite (планируется миграция на PostgreSQL)
- REST API (80+ endpoints)
- Uvicorn server

### Frontend
- Next.js 14 (App Router)
- TypeScript
- TailwindCSS
- React 18

### ML & Trading
- XGBoost, LightGBM, CatBoost
- Pandas, NumPy для обработки данных
- Stable-Baselines3 для RL (опционально)
- **НЕ используется**: PyTorch deep learning, TensorFlow

### Testing
- pytest (127 tests, ~85% coverage)
- Arrange-Act-Assert pattern
- Моки для внешних API

### Infrastructure
- Docker Compose (MLflow, Prometheus, Grafana)
- Alembic для миграций
- Sentry для мониторинга ошибок
- Healthchecks.io

## ⚠️ Специфика проекта
### Trading & ML
- Используем EMA Crossover стратегию (Sharpe 3.11!)
- ML модели для вспомогательных сигналов (XGBoost, не deep learning)
- Paper Trading режим на Bybit
- Risk Management: Stop-Loss, Take-Profit, Trailing Stop

### API Validation
- Всегда валидировать изменения через Swagger UI (http://localhost:8000/docs)
- Проверять контракты API (не ломать backwards compatibility)
- Тестировать эндпоинты с разными данными (success + error cases)

### Безопасность
- API_KEY обязателен для всех эндпоинтов
- Секреты только в .env файлах
- Не коммитить API keys, tokens, passwords
- Проверять лицензии зависимостей

## 📖 Дополнительные технические правила
Смотри также:
- `.cursor/rules/01-python.mdc` — Python best practices
- `.cursor/rules/04-fastapi.mdc` — FastAPI patterns
- `.cursor/rules/06-rest-api.mdc` — REST API design
- `.cursor/rules/08-testing.mdc` — Testing strategy
- `.cursor/rules/10-nextjs.mdc` — Next.js App Router
- `.cursor/rules/11-react.mdc` — React patterns
- `.cursor/rules/12-tailwindcss.mdc` — TailwindCSS conventions

